1. Make changes in any branch.
2. Merge to main (for consistency).
3. run: `npm run build` to create dist
4. run: `npm run deploy` to deploy
5. site available at `https://siddharthkoli.github.io/stat-tracker/`